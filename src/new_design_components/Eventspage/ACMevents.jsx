import React from 'react';
// import { 
//   codex, 
//   aiml, 
//   cookthecode, 
//   prashanth, 
//   espblast, 
//   shouryaRoy, 
//   summerBoost,
//   esp_in_analytics,
//   esp_on_ioe,
//   esp_on_security,
//   fun_with_algorithms,
//   hackare,
//   hack_odyssey,
//   hour_of_codes,
//   idea_spark_1,
//   idea_sparks_2
// } from './../../constants/data.js'; 
// import { Carousel } from 'react-responsive-carousel';
// import 'react-responsive-carousel/lib/styles/carousel.min.css';
// import styles from './ACMEvents.css'; 

function ACMevents() {
  // const renderCarousel = (eventData, eventName) => (
  //   <div className={styles.eventSection}>
  //     <h2>{eventName}</h2>
  //     <Carousel className={styles.carousel} showThumbs={false} infiniteLoop autoPlay>
  //       {eventData.map((event) => (
  //         <div key={event.id} className={styles.carouselItem}>
  //           <img src={event.image} alt={`${eventName} ${event.id}`} />
  //         </div>
  //       ))}
  //     </Carousel>
  //   </div>
  // );

  return (
    <>

      {/* <div className={styles.container}>
        <h1>Previous events | Upcoming events</h1>

        <div className={styles.eventsWrapper}>
          <h1>Previous Events</h1>

          {renderCarousel(codex, 'Code Sprint')}
          {renderCarousel(aiml, 'AI/ML Conversations')}
          {renderCarousel(cookthecode, 'Cook the Code')}
          {renderCarousel(prashanth, 'Prashanth\'s Event')}
          {renderCarousel(espblast, 'ESP Blast')}
          {renderCarousel(shouryaRoy, 'Shourya Roy Speech')}
          {renderCarousel(summerBoost, 'Summer Boost Program')}
          {renderCarousel(esp_in_analytics, 'ESP in Analytics')}
          {renderCarousel(esp_on_ioe, 'ESP on IoE')}
          {renderCarousel(esp_on_security, 'ESP on Security')}
          {renderCarousel(fun_with_algorithms, 'Fun with Algorithms')}
          {renderCarousel(hackare, 'Hackare')}
          {renderCarousel(hack_odyssey, 'Hack Odyssey')}
          {renderCarousel(hour_of_codes, 'Hour of Code')}
          {renderCarousel(idea_spark_1, 'Idea Sparks - Round 1')}
          {renderCarousel(idea_sparks_2, 'Idea Sparks - Round 2')}

        </div>
      </div> */}
    </>
  );
}

export default ACMevents;
